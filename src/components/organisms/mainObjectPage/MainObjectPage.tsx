import React from 'react';

const MainObjectPage = ({ objectName }:any) => {
    return (
        <div>
            <h1>Объект: {objectName}</h1>
        </div>
    );
};

export default MainObjectPage;