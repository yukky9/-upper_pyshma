import React from 'react';
import PreviewCard from '../molecules/cards/PreviewCard';
import Header from "../templates/Header";

const GeneralPage = () => {
    return (
        <div>
            <Header/>
            <PreviewCard/>
        </div>
    );
};

export default GeneralPage;